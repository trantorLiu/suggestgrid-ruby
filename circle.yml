machine:
  pre:
    - sudo apt-get update
    - sudo apt-get install expect
test:
  override:
    - echo "test"
deployment:
  master:
    branch: master
    owner: suggestgrid
    commands:
      - ./.deploy
